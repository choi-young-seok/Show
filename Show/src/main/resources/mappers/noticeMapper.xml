<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC " -//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="notice">
  
   <!-- 전체 레코드 조회 -->
   <select id="listAll" resultType="kr.co.show.community.domain.NoticeVO">
      select notice_no,notice_title,notice_text,notice_ch,notice_regdate
      from show_notice
      order by notice_no desc
   </select>

   <!-- 한페이지당 10개의 게시물, 2번째 페이지 보이기 -->
   <select id="listPage" resultType="kr.co.show.community.domain.NoticeVO">
      select notice_no,notice_title,notice_text,notice_ch,notice_regdate
      from (select notice_no,notice_title,notice_text,notice_ch,notice_regdate, rownum rn
		      from (select notice_no,notice_title,notice_text,notice_ch,notice_regdate
		            from show_notice
		            order by notice_no desc))
      where rn between 11 and 20      
   </select>
   
    <select id="noticeTotalCount" resultType="int">
       select count(*) notice_no
   </select>
</mapper>
